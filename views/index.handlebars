<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>index</title>
</head>
<body>
	<h1>{{title}}</h1>

	{{#each map}}
		<h2>{{cat}}</h2>
		<ul>
			{{#each list}}
				<li><a href="/{{../cat}}/{{this}}">{{this}}</a></li>
			{{/each}}
		</ul>
	{{/each}}
</body>
</html>